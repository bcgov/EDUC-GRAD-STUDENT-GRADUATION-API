UPDATE ALGORITHM_RULE_CODE SET DESCRIPTION='Checks if the student achievement record (e.g., course) session date has passed (e.g. earlier than today - YYYYMM)  and no final result or special case exists.' WHERE ALGORITHM_RULE_CODE = 'INCOMPLETE_CRSE_RULE';
UPDATE ALGORITHM_RULE_CODE SET DESCRIPTION='Checks if the student achievement record (e.g., course) session date is still current or future dated (e.g. from today YYYYMM) or later) and no final result or special case exists.' WHERE ALGORITHM_RULE_CODE = 'CRSE_REGISTRATION_RULE';
UPDATE ALGORITHM_RULE_CODE SET DESCRIPTION='Checks if the student achievement record (e.g. assessment) session date has passed (e.g. earlier than today - YYYMM) and no final result or special case exists.' WHERE ALGORITHM_RULE_CODE = 'INCOMPLETE_ASSMT_RULE';
UPDATE ALGORITHM_RULE_CODE SET DESCRIPTION='Checks if the student achievement record (e.g. assessment) session date is still current or future dated (e.g., from today YYYYMM) or later) and no final result or special case exists.' WHERE ALGORITHM_RULE_CODE = 'ASSMT_REGISTRATION_RULE';